apiVersion: v1
kind: Service
metadata:
  name: node-health
  namespace: {{ .Values.namespace }}
  labels:
    app: node-health
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
  - port: 8000
  selector:
    app: node-health
